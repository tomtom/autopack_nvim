# Closed Issues

1. In register(): Add patterns as additional trigger. When a file matching one of the patterns then the opt pack is loaded. #closed/2026-06-14

2. Describe the plugins functionality in docs/autopack.txt. Adhere to the format of neovim help files. #closed/2026-06-14

vim: set ft=markdown.pandoc tw=0 ts=4 :
